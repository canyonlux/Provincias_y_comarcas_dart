
import 'package:flutter/material.dart';

class ComarcasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Lista de Comarcas'),
      ),
      body: Center(
        child: Text('Aquí se mostrará la lista de comarcas'),
      ),
    );
  }
}
